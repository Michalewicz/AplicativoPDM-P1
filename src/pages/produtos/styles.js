import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    tituloNotFound:{
      fontSize: 50,
      marginTop: 50,
      textAlign: 'center',
      color:'red',
    },
    borda:{
      borderColor:'black',
      borderWidth: 10,
      flex:1,
    },
});


export {styles}
