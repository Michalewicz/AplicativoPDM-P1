import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  headerContainer: {
    alignItems: 'center',
    alignContent: 'center',
    backgroundColor: '#a20000',
  },
  logo: {
    width: 275,
    height: 125,
    marginTop: 10,
    alignSelf: 'center',
  },
});

export { styles };
